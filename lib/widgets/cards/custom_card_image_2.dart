import 'package:flutter/material.dart';

class CustomCardImage2 extends StatelessWidget {
  const CustomCardImage2({super.key});

  @override
  Widget build(BuildContext context) {
    return Card(
      child: Image.asset("assets/imgs/jar-loading.gif"),
    );
  }
}
